 * 遇到的反爬虫
     * robot.txt
     * user-agent
     * cookie加密
     * ip 封禁
  
  * 验证码怎么破解 
  
  * 知道CNN技术吗
    * 答了 卷积神经网络
   
   * ~~布隆过滤器误判率~~
   * 为什么会上布隆过滤器
   * mysql 查询优化是怎么优化的
       * 用between and 如果用 * 就可以走主键索引
       * 但是使用limit 去 * 不符合最左前缀 就会导致不走索引 全量查询
   * 用了ORM吗 用什么连接驱动
       * 用了mysqldb  没有pymysql
   * DB里面的事务 
        讲了事务隔离机制
  * 将一下未提交读
  * 讲一下串行读  面试官讲了秒杀场景 可以适用 
  * 爬虫的DFS 有遇到循环的问题吗
  * 为什么没有用BFS 用的DFS
  * DFS 栈溢出 问题
  * 布隆过滤器bit数 hash数目是怎么选择的
      * bit 9000w 100M hash 数目
  * 1亿的请求的log 请求独立的ip数目 
   * 聊一下java反射
   * 你直接的 server 打算支持 HTTPS
   * Django ORM 了解吗  讲一下ORM
   * ORM N+1 遇到过吗
   * elasticsearch 了解过吗
   * 爬虫有爬html吗
   * 怎么做增量
   * 增量怎么才能避免全量爬
